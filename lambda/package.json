{
  "name": "orderMailer",
  "version": "1.0.0",
  "description": "A serverless event-driven mail sender.",
  "main": "index.js",
  "scripts": {
    "lambda:dependencies": "cd ./src && npm install && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npm run lambda:dependencies && tsc && cp -rv src/node_modules dist/src/node_modules",
    "deploy": "tsc && cdk deploy",
    "test": "npm run compile && node test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-cdk/aws-apigateway": "^1.38.0",
    "@aws-cdk/aws-dynamodb": "^1.38.0",
    "@aws-cdk/aws-lambda-event-sources": "^1.38.0",
    "@aws-cdk/aws-sqs": "^1.38.0",
    "@types/aws-lambda": "^8.10.51",
    "@types/node": "^13.13.5",
    "@types/nodemailer": "^6.4.0",
    "aws-sdk": "^2.673.0",
    "dotenv": "^8.2.0",
    "nodemailer": "^6.4.6",
    "typescript": "^3.6.3"
  }
}
